# 🚀 Configuration de l'application PFE Backend
# Copiez ce fichier vers .env et modifiez les valeurs selon votre environnement

# =============================================================================
# CONFIGURATION DE BASE
# =============================================================================
NODE_ENV=development
PORT=3000

# =============================================================================
# BASE DE DONNÉES POSTGRESQL
# =============================================================================
# Format: postgresql://username:password@host:port/database
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/pfe_db
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=pfe_db

# =============================================================================
# RABBITMQ - MESSAGE BROKER
# =============================================================================
# Format: amqp://username:password@host:port
RABBITMQ_URL=amqp://guest:guest@localhost:5672
RABBITMQ_USER=guest
RABBITMQ_PASSWORD=guest

# =============================================================================
# INFLUXDB - BASE DE DONNÉES TEMPS RÉEL
# =============================================================================
INFLUXDB_URL=http://localhost:8086
INFLUXDB_TOKEN=adminpassword
INFLUXDB_ORG=pfe_org
INFLUXDB_BUCKET=pfe_bucket
INFLUXDB_USERNAME=admin
INFLUXDB_PASSWORD=adminpassword

# =============================================================================
# SÉCURITÉ JWT
# =============================================================================
# ⚠️ IMPORTANT: Changez cette valeur en production !
# Générez une clé sécurisée avec: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# =============================================================================
# DÉPLOIEMENT AZURE (pour GitHub Actions - optionnel)
# =============================================================================
# Ces variables ne sont utilisées que si le déploiement Azure est activé
# dans le workflow GitHub Actions
AZURE_VM_HOST=your-azure-vm-ip
AZURE_VM_USERNAME=azureuser
AZURE_VM_SSH_KEY=your-ssh-private-key

# =============================================================================
# GITHUB CONTAINER REGISTRY
# =============================================================================
# Nom d'utilisateur GitHub pour publier les images Docker
GITHUB_REPOSITORY_OWNER=enzoparriel

# =============================================================================
# CONFIGURATIONS AVANCÉES (optionnelles)
# =============================================================================
# Logs
LOG_LEVEL=info

# CORS
CORS_ORIGIN=http://localhost:3000

# Rate Limiting
THROTTLE_TTL=60
THROTTLE_LIMIT=100

# =============================================================================
# NOTES DE SÉCURITÉ
# =============================================================================
# 🔒 En production, assurez-vous de :
# 1. Changer tous les mots de passe par défaut
# 2. Utiliser des clés JWT sécurisées et uniques
# 3. Limiter l'accès aux ports de base de données
# 4. Configurer des certificats SSL/TLS
# 5. Utiliser des variables d'environnement sécurisées
# 6. Ne jamais commiter le fichier .env dans Git 